extends layout

block content
    
     div(class="row my-3")
        div(class="col-md-4 py-1")
            h2 Group Information

            form()
                div(class="form-group")
                    label(for="groupLevel1Name" ) Group Level 1:
                    input(type="text", class="form-control", id="groupLevel1Name")
                    small(id="groupLevel1NameHelp", class="form-text text-muted") This would be a configurable Label

                    label(for=ownerEmail) Owner Email:
                    input(type="text", class="form-control", id="ownerEmail")
                    small(id="ownerEmailHelp", class="form-text text-muted") Email Address for the owner of this group level

                    label(for=teamEmail) Team Email:
                    input(type="text", class="form-control", id="teamEmail")
                    small(id="teamEmailHelp", class="form-text text-muted") Email Address for the team of this group level

                    h2 Available Cost Codes     
                        ul()
                            li Cost Code 1
                            li Cost Code 2
                            li Cost Code n

                    h2 2nd Level Groups (configurable label)
                    
                        ul()
                            li Group 1
                            li Group n
                    
                    
                    Tag Transition

                        Replace IO139098 with IO280281
                    
                    p
                        Labels would be configurable